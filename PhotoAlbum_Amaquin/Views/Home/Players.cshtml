@model Dictionary<string,List<Players>>

<section class="hero-section players-page-hero text-white d-flex flex-column justify-content-center align-items-end">
    <div class="px-4 py-2 w-50">
        <h2 class="font-bold alter-font-family">
            “DESTROY YOUR PEACEFUL LIFE
            WITH THIS STUPID GAME.”
        </h2>
        <p class="color-secondary opacity-75 section-sublable">
            - Trust me, I have spent thousands of hours and money.
        </p>
    </div>
</section>

<section class="container text-white my-5">
    <h3 class="section-label text-center">2023 TI Champions</h3>
    <p class="section-sublable text-start mb-4">
        Lorem lorem lorem
    </p>
    <div class="d-flex flex-wrap align-items-center justify-content-evenly gap-3">
        @foreach(var player in Model.GetValueOrDefault("TI",new List<Players>()))
        {
            <div class="dota-player-card">
                <h4 class="mb-3 text-center text-bolder text-lg">@player.Name</h4>
                <div class="player-image">
                    <img src="@Url.Content(player.ImageSource)" alt=@player.Name />
                </div>
                <div class="p-2">
                    <p class="text-md mb-0">Real Name: <span class="text-bolder text-sm">@player.RealName</span></p>
                    <p class="text-md mb-0">Team Role: <span class="text-bolder text-sm">@player.TeamRole</span></p>
                    <p class="text-md mb-0">Approx Winning: <span class="text-bolder text-sm">@player.ApproxWinning</span></p>
                </div>
                <div class="p-1">
                    <p class="mb-0 px-3 py-2 bg-midnight-blue-color">
                        @player.Description
                    </p>
                </div>
            </div>
        }
    </div>
</section>

<section class="container text-white my-5">
    <h3 class="section-label text-center">Filipino Pro Players</h3>
    <p class="section-sublable text-start mb-4">
        Lorem lorem lorem
    </p>
    <div class="d-flex flex-wrap align-items-center justify-content-evenly gap-3">
        @foreach (var player in Model.GetValueOrDefault("Filipino", new List<Players>()))
        {
            <div class="dota-player-card">
                <h4 class="mb-3 text-center text-bolder text-lg">@player.Name</h4>
                <div class="player-image">
                    <img src="@Url.Content(player.ImageSource)" alt=@player.Name />
                </div>
                <div class="p-2">
                    <p class="text-md mb-0">Real Name: <span class="text-bolder text-sm">@player.RealName</span></p>
                    <p class="text-md mb-0">Team Role: <span class="text-bolder text-sm">@player.TeamRole</span></p>
                    <p class="text-md mb-0">Approx Winning: <span class="text-bolder text-sm">@player.ApproxWinning</span></p>
                </div>
                <div class="p-1">
                    <p class="mb-0 px-3 py-2 bg-midnight-blue-color">
                        @player.Description
                    </p>
                </div>
            </div>
        }
    </div>
</section>

<section class="container text-white my-5">
    <h3 class="section-label text-center">Chinese Pro Players</h3>
    <p class="section-sublable text-start mb-4">
        Lorem lorem lorem
    </p>
    <div class="d-flex flex-wrap align-items-center justify-content-evenly gap-3">
        @foreach (var player in Model.GetValueOrDefault("Chinese", new List<Players>()))
        {
            <div class="dota-player-card">
                <h4 class="mb-3 text-center text-bolder text-lg">@player.Name</h4>
                <div class="player-image">
                    <img src="@Url.Content(player.ImageSource)" alt=@player.Name />
                </div>
                <div class="p-2">
                    <p class="text-md mb-0">Real Name: <span class="text-bolder text-sm">@player.RealName</span></p>
                    <p class="text-md mb-0">Team Role: <span class="text-bolder text-sm">@player.TeamRole</span></p>
                    <p class="text-md mb-0">Approx Winning: <span class="text-bolder text-sm">@player.ApproxWinning</span></p>
                </div>
                <div class="p-1">
                    <p class="mb-0 px-3 py-2 bg-midnight-blue-color">
                        @player.Description
                    </p>
                </div>
            </div>
        }
    </div>
</section>

<section class="container text-white my-5">
    <h3 class="section-label text-center">Banned Pro Players</h3>
    <p class="section-sublable text-start mb-4">
        Lorem lorem lorem
    </p>
    <div class="d-flex flex-wrap align-items-center justify-content-evenly gap-3">
        @foreach (var player in Model.GetValueOrDefault("Banned", new List<Players>()))
        {
            <div class="dota-player-card">
                <h4 class="mb-3 text-center text-bolder text-lg">@player.Name</h4>
                <div class="player-image">
                    <img src="@Url.Content(player.ImageSource)" alt=@player.Name />
                </div>
                <div class="p-2">
                    <p class="text-md mb-0">Real Name: <span class="text-bolder text-sm">@player.RealName</span></p>
                    <p class="text-md mb-0">Team Role: <span class="text-bolder text-sm">@player.TeamRole</span></p>
                    <p class="text-md mb-0">Approx Winning: <span class="text-bolder text-sm">@player.ApproxWinning</span></p>
                </div>
                <div class="p-1">
                    <p class="mb-0 px-3 py-2 bg-midnight-blue-color">
                        @player.Description
                    </p>
                </div>
            </div>
        }
    </div>
</section>